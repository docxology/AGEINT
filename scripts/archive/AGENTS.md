# AGENTS.md - scripts/archive/

Historical migration helpers live here for reference only. Do not use these
scripts in the normal AGEINT build path unless a project-local maintainer asks
for a legacy migration.

Before touching this directory, read the project root `AGENTS.md` and
`scripts/README.md`. Keep new operational scripts in `scripts/`, not here.
