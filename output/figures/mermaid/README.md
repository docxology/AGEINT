# Mermaid Figure Sources

This generated directory stores local `.mmd` source files and square-normalized PNG renders for curriculum and part maps. Rebuild from `src/figures/` rather than editing rendered images or generated Mermaid by hand.
