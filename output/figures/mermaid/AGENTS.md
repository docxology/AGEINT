# AGENTS.md - Mermaid Figure Sources

Keep Mermaid diagrams legible, registry-backed, and roughly square after rendering. Use quoted labels, avoid hard-coded figure numbers, and verify that each `.mmd` source has a paired PNG listed in `../figure_registry.json`.
