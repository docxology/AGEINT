# Gray Zone Hybrid Warfare And Non State Actors

**Evidence anchor.** [@sec:part-gray-zone-hybrid-warfare-and-non-state-actors].

This generated part folder contains `unit_intro.md` and the chapter modules for one AGEINT curriculum part. The files are rendered from `data/curriculum/`, neutral source templates, profile-specific scholarship anchors, and the manifest renderer.

Edit the source data, templates, or renderer and rebuild with `uv run python scripts/build_curriculum.py` from the AGEINT root; do not treat generated Markdown as the primary source of truth.
