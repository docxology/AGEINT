# source_identity

Locked source identities for the 231 AGEINT source records (`ageint001`-`ageint231`), stored as JSONL shards under `lock/` plus `metadata.json`. Preserving these identity keys is required when adding or extending citations.
